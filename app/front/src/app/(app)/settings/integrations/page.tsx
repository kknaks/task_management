"use client";

/**
 * P-06 연동 관리 — **본문은 다음 배치가 채운다.** v1 에서는 기능 자체가 비활성이다
 * (DEC-001 §v2 · SPEC-001 §1 Out of scope).
 */

export default function Page() {
  return <h1 className="text-page-title text-foreground">연동 관리</h1>;
}
