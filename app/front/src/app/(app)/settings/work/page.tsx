"use client";

/**
 * 업무 설정(유형·프로젝트 관리) — **본문은 WORK-003 이 채운다.**
 *
 * 이 파일은 U-4 의 「항목이 보이고 이동은 되지만 본문이 비어 있다」를 만족시키는 껍데기다.
 * 화면 규격은 §B-1 디자인 대기이고 라우트만 확정돼 있다(FE §1).
 */

export default function Page() {
  return <h1 className="text-page-title text-foreground">업무 설정</h1>;
}
