"use client";

/**
 * P-05 개인 설정 — **이 배치에서는 본문이 비어 있다.**
 *
 * 항목이 보이고 이동은 되지만 프로필·경력·목소리 패널은 **개인 설정 그룹(SPEC-010)** 의 몫이다
 * (SPEC-001 §1 Out of scope · U-4 CTA). 여기서 미리 만들지 않는다.
 */

export default function Page() {
  return <h1 className="text-page-title text-foreground">개인 설정</h1>;
}
