# 프론트 env 예시. `cp .env.example .env.local` 후 값을 채운다.
# 키 이름은 SPEC-000 §5 「환경변수」 표를 따른다.
#
# `NEXT_PUBLIC_*` 는 **빌드 시점에 번들로 박힌다** — 앱 안에서 바꾸는 설정 화면은 v1 에 없다
# (SYS §런타임 배치 · DEC-001 §1). 값이 없으면 빌드·기동이 선다.
#
# 이 값을 읽는 곳은 `src/lib/env.ts` **하나뿐**이다.

NEXT_PUBLIC_API_BASE=http://localhost:8000
